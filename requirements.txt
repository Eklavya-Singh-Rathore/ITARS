# ITARS V2 backend — pinned for Python 3.11.
# scikit-learn is pinned to the version the serving artifacts were saved with
# (1.5.1) to avoid InconsistentVersionWarning / unpickle drift (audit defect #12).

# --- core ML / serving stack ---
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
pandas==2.2.2
joblib==1.4.2
xgboost==2.1.1
faiss-cpu==1.8.0.post1

# --- embeddings / transformers ---
sentence-transformers==5.2.3
transformers==4.44.2
torch==2.4.1
huggingface-hub==0.24.6

# --- translation (Phase 3: langdetect + MarianMT) ---
langdetect==1.0.9
sentencepiece==0.2.0
sacremoses==0.1.1

# --- config / serialization ---
PyYAML==6.0.2

# --- database (Phase 6 / 15A) ---
SQLAlchemy==2.0.50
# Postgres driver — needed for Supabase / production; SQLite still works without
# it (sqlite3 ships with CPython). Pinned to a current psycopg v3 release.
psycopg[binary]==3.2.3

# --- RAG (Phase 7 / 15B): retrieval-only vector store ---
# Vectors live in Supabase pgvector (same Postgres as the relational data, via
# SQLAlchemy + psycopg above). No separate vector-DB client needed.
# Embeddings: BGE-small via sentence-transformers (above).

# --- LLM gateway (Phase 8): providers call Gemini/Grok REST via httpx (no SDK) ---
httpx==0.27.2

# --- API layer (Phase 2) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- testing ---
pytest==8.3.3
